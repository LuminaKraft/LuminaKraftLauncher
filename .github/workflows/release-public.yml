# ❌ DEPRECATED - This workflow is now unused
# The build process has been moved to the public repository for FREE GitHub Actions minutes
# See: https://github.com/kristiangarcia/luminakraft-launcher-releases

name: "[DEPRECATED] Build and Release - Moved to Public Repo"

on:
  # Disabled - use push-to-public.yml instead
  workflow_dispatch:

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "❌ This workflow is DEPRECATED"
          echo "🔄 Builds are now handled by the public repository:"
          echo "   https://github.com/kristiangarcia/luminakraft-launcher-releases"
          echo ""
          echo "💡 Benefits of the new system:"
          echo "   ✅ FREE GitHub Actions minutes (unlimited for public repos)"
          echo "   ✅ Faster builds with better caching"
          echo "   ✅ Direct releases to public repository"
          echo ""
          echo "🚀 Use push-to-public.yml workflow instead" 
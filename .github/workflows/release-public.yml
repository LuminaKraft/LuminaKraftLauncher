name: Build and Release (Multi-Platform) - FIXED

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: x86_64-pc-windows-msvc

      - name: Cache Rust dependencies (excluding build artifacts)
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-v2
          restore-keys: |
            ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Clean previous builds (prevents version conflicts)
        run: |
          echo "🧹 Cleaning previous builds to prevent version conflicts..."
          if (Test-Path "src-tauri/target/release/bundle/") { 
            Write-Host "Removing release bundle..."
            Remove-Item -Recurse -Force "src-tauri/target/release/bundle/" 
          }
          if (Test-Path "src-tauri/target/debug/bundle/") { 
            Write-Host "Removing debug bundle..."
            Remove-Item -Recurse -Force "src-tauri/target/debug/bundle/" 
          }
          echo "✅ Build cache cleaned"

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build

      - name: Build Tauri app
        run: npx tauri build
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}

      - name: List generated files (for debugging)
        run: |
          echo "=== Windows Build Artifacts ==="
          if (Test-Path "src-tauri/target/release/bundle/msi/") {
            Get-ChildItem "src-tauri/target/release/bundle/msi/" -Name
          }
          if (Test-Path "src-tauri/target/release/bundle/nsis/") {
            Get-ChildItem "src-tauri/target/release/bundle/nsis/" -Name
          }

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: |
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: error

  build-linux:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: x86_64-unknown-linux-gnu

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            build-essential \
            curl \
            wget \
            file \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            libxdo-dev

      - name: Cache Rust dependencies (excluding build artifacts)
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-v2
          restore-keys: |
            ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Clean previous builds (prevents version conflicts)
        run: |
          echo "🧹 Cleaning previous builds to prevent version conflicts..."
          rm -rf src-tauri/target/release/bundle/ || true
          rm -rf src-tauri/target/debug/bundle/ || true
          echo "✅ Build cache cleaned"

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build

      - name: Build Tauri app
        run: npx tauri build
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}

      - name: List generated files (for debugging)
        run: |
          echo "=== Linux Build Artifacts ==="
          find src-tauri/target/release/bundle/ -name "*.AppImage" -o -name "*.deb" -o -name "*.rpm" 2>/dev/null | sort || true

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-build
          path: |
            src-tauri/target/release/bundle/appimage/*.AppImage
            src-tauri/target/release/bundle/deb/*.deb
            src-tauri/target/release/bundle/rpm/*.rpm
          if-no-files-found: error

  build-macos:
    runs-on: macos-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: |
            x86_64-apple-darwin
            aarch64-apple-darwin

      - name: Cache Rust dependencies (excluding build artifacts)
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-v2
          restore-keys: |
            ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Clean previous builds (prevents version conflicts)
        run: |
          echo "🧹 Cleaning previous builds to prevent version conflicts..."
          rm -rf src-tauri/target/*/release/bundle/ || true
          rm -rf src-tauri/target/*/debug/bundle/ || true
          echo "✅ Build cache cleaned"

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build

      - name: Configure macOS build (no code signing for testing)
        run: |
          mkdir -p ~/.tauri
          cat > ~/.tauri/settings.json << EOL
          {
            "macOSCodeSign": {
              "identity": null,
              "hardened": false
            }
          }
          EOL

      - name: Build Tauri app (Apple Silicon - ARM64)
        run: npx tauri build --target aarch64-apple-darwin
        env:
          TAURI_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}
          MACOSX_DEPLOYMENT_TARGET: "10.13"

      - name: Build Tauri app (Intel - x86_64)
        run: npx tauri build --target x86_64-apple-darwin
        env:
          TAURI_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}
          MACOSX_DEPLOYMENT_TARGET: "10.13"

      - name: List generated files (for debugging)
        run: |
          echo "=== macOS Build Artifacts ==="
          find src-tauri/target/*/release/bundle/ -name "*.dmg" -o -name "*.app" 2>/dev/null | sort || true

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-build
          path: |
            src-tauri/target/x86_64-apple-darwin/release/bundle/dmg/*.dmg
            src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/*.dmg
            src-tauri/target/x86_64-apple-darwin/release/bundle/macos/*.app
            src-tauri/target/aarch64-apple-darwin/release/bundle/macos/*.app
          if-no-files-found: error

  create-release:
    needs: [build-windows, build-linux, build-macos]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get version from tag
        id: get_version
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"
          
          # Check if this is a pre-release from package.json
          IS_PRERELEASE=$(node -p "JSON.parse(require('fs').readFileSync('package.json', 'utf8')).isPrerelease || false")
          echo "IS_PRERELEASE=$IS_PRERELEASE" >> $GITHUB_OUTPUT
          
          echo "🔍 Debug package.json isPrerelease field:"
          node -p "const pkg = JSON.parse(require('fs').readFileSync('package.json', 'utf8')); console.log('isPrerelease:', pkg.isPrerelease, 'type:', typeof pkg.isPrerelease); pkg.isPrerelease"
          
          if [[ "$IS_PRERELEASE" == "true" ]]; then
            echo "✅ This is a PRE-RELEASE: $VERSION"
          else
            echo "✅ This is a STABLE release: $VERSION"
          fi

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: List downloaded artifacts (for debugging version conflicts)
        run: |
          echo "=== All Downloaded Artifacts ==="
          find artifacts/ -type f -name "*.msi" -o -name "*.exe" -o -name "*.AppImage" -o -name "*.deb" -o -name "*.rpm" -o -name "*.dmg" | sort
          echo ""
          echo "🔍 Checking for version conflicts:"
          echo "Files with correct version (${{ steps.get_version.outputs.VERSION }}):"
          find artifacts/ -name "*${{ steps.get_version.outputs.VERSION }}*" | sort
          echo ""
          echo "❌ Files with INCORRECT versions (should be investigated):"
          find artifacts/ -type f \( -name "*.msi" -o -name "*.exe" -o -name "*.AppImage" -o -name "*.deb" -o -name "*.rpm" -o -name "*.dmg" \) ! -name "*${{ steps.get_version.outputs.VERSION }}*" | sort

      - name: Create Release in Public Repository
        id: create_public_release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.PUBLIC_REPO_TOKEN }}
        with:
          repository: kristiangarcia/luminakraft-launcher-releases
          tag_name: ${{ github.ref_name }}
          name: "🚀 LuminaKraft Launcher ${{ github.ref_name }}"
          body: |
            ## 📥 Instrucciones de Descarga
            
            ${{ steps.get_version.outputs.IS_PRERELEASE == 'true' && '🧪 **Versión Pre-Release** - Esta es una versión de prueba con características experimentales' || '🎉 **Versión Estable** - Versión lista para producción' }}
            
            ### 🪟 **Windows**
            - **MSI Installer** (`*.msi`) - Recomendado
            - **NSIS Installer** (`*.exe`) - Alternativo
            
            ### 🐧 **Linux**
            - **AppImage** (`*.AppImage`) - Recomendado (portable)
            - **DEB Package** (`*.deb`) - Debian/Ubuntu
            - **RPM Package** (`*.rpm`) - Red Hat/Fedora
            
            ### 🍎 **macOS**
            - **Apple Silicon** (`aarch64-apple-darwin.dmg`) - M1/M2/M3/M4
            - **Intel Macs** (`x86_64-apple-darwin.dmg`) - Macs Intel
            
            ## 🔗 Enlaces
            - 💬 **Discord**: [Únete a nuestra comunidad](https://discord.gg/UJZRrcUFMj)
            - 🐛 **Reportar bugs**: [GitHub Issues](https://github.com/kristiangarcia/luminakraft-launcher-releases/issues)
            
            ${{ steps.get_version.outputs.IS_PRERELEASE == 'true' && '⚠️ **Advertencia**: Esta versión puede contener errores. Úsala bajo tu propio riesgo.' || '✅ **Versión estable y recomendada para todos los usuarios.**' }}
          draft: false
          prerelease: ${{ steps.get_version.outputs.IS_PRERELEASE == 'true' }}
          files: |
            artifacts/**/*.msi
            artifacts/**/*.exe
            artifacts/**/*.AppImage
            artifacts/**/*.deb
            artifacts/**/*.rpm
            artifacts/**/*.dmg

      - name: Create Release in Private Repository (Internal Tracking)
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          name: "📋 Internal Release Tracking ${{ github.ref_name }}"
          body: |
            ## 🔗 **Release Público**
            **🌐 Descarga**: https://github.com/kristiangarcia/luminakraft-launcher-releases/releases/tag/${{ github.ref_name }}
            
            ## 🏗️ **Info de Build**
            - **Versión**: `${{ steps.get_version.outputs.VERSION }}`
            - **Commit**: `${{ github.sha }}`
            - **Pre-release**: `${{ steps.get_version.outputs.IS_PRERELEASE }}`
            
            ### 📦 **Builds Completados**
            - ✅ **Windows**: MSI + NSIS
            - ✅ **Linux**: AppImage + DEB + RPM  
            - ✅ **macOS**: DMG (ARM64 + x86_64)
            
            ${{ steps.get_version.outputs.IS_PRERELEASE == 'true' && '🧪 **PRE-RELEASE** - Versión de prueba' || '🎉 **RELEASE ESTABLE** - Versión de producción' }}
            
            ---
            **🔒 Solo uso interno** - Tracking para el equipo de desarrollo.
          draft: false
          prerelease: ${{ steps.get_version.outputs.IS_PRERELEASE == 'true' }}
        continue-on-error: true

      - name: Build Summary
        run: |
          echo "=== 🎉 Release Build Summary ==="
          echo "✅ Version: ${{ steps.get_version.outputs.VERSION }}"
          echo "✅ Pre-release: ${{ steps.get_version.outputs.IS_PRERELEASE }}"
          echo "✅ Public release: https://github.com/kristiangarcia/luminakraft-launcher-releases/releases/tag/${{ github.ref_name }}"
          echo "✅ Private tracking: https://github.com/kristiangarcia/luminakraft-launcher/releases/tag/${{ github.ref_name }}"
          echo ""
          echo "🏗️ **Built Platforms:**"
          echo "  🪟 Windows: MSI + NSIS installers"
          echo "  🐧 Linux: AppImage + DEB + RPM packages"
          echo "  🍎 macOS: DMG for ARM64 + x86_64"
          echo ""
          echo "🔧 **Cache Fixes Applied:**"
          echo "  ✅ Build artifacts cleaned before build"
          echo "  ✅ Cache keys improved to prevent conflicts"
          echo "  ✅ Debugging output added for artifact verification"
          echo ""
          echo "🎯 **Release completed successfully!**" 
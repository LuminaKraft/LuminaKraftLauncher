# ⚠️ DEPRECATED - Este workflow ya no se usa
# Los releases ahora se manejan directamente en el repositorio público:
# https://github.com/kristiangarcia/luminakraft-launcher-releases

name: "⚠️ [DEPRECATED] Sync to Public Repository"

on:
  # Workflow deshabilitado - usar git push directamente
  workflow_dispatch:
    inputs:
      force_push:
        description: 'Este workflow está deprecado'
        required: false
        type: boolean
        default: false

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: ⚠️ Workflow Deprecado
        run: |
          echo "❌ Este workflow ya no se usa"
          echo ""
          echo "✨ Nuevo proceso de release:"
          echo "1. Hacer cambios en el repositorio privado"
          echo "2. git push origin main"
          echo "3. git push public main"
          echo ""
          echo "🔗 Los builds se ejecutan automáticamente en:"
          echo "https://github.com/kristiangarcia/luminakraft-launcher-releases"
          exit 1 